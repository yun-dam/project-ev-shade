# Pinned to the versions this project was built and tested against (Python 3.11).
# Install:  pip install -r requirements.txt
google-genai==2.8.0
pandas==3.0.3
pyarrow==24.0.0
pillow==12.2.0
tqdm==4.67.3
python-dotenv==1.2.2
requests==2.32.3  # ground-level image retrieval (src/retrieve_ground.py)

# NOTE: the gold-set labeling app (gold/label_app.py) uses only the Python standard
# library, so a labmate can label WITHOUT any of the above except a Python 3.x install.
# The packages above are needed for the model/classification side (src/, gold/evaluate.py).
